{
    "scripts": {
        "format": "prettier --write .",
        "check-format": "prettier --check ."
    },
    "dependencies": {
        "@tailwindcss/cli": "^4.1.10",
        "tailwindcss": "^4.0.0"
    },
    "devDependencies": {
        "prettier": "^3.4.2"
    }
}
