{
    "scripts": {
        "format": "prettier --write .",
        "check-format": "prettier --check ."
    },
    "dependencies": {
        "tailwindcss": "^4.0.0"
    },
    "devDependencies": {
        "prettier": "^3.4.2"
    }
}
